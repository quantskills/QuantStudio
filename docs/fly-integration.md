# 果蝇交易员接入约定

用户确认范围：完整果蝇神经运行、3D 家园、交流、学习和交易统计；首版支持 Windows，面向独立安装 QuantStudio 的用户。源码来自 `songshuquant/ssquant-fly-agent` 的 `codex/fly-autonomous-v2`。

交易复用 PandaAI 比赛 CLI，行情复用 PandaData。首版生成冻结计划，由用户在现有比赛确认卡逐笔执行。赛事端尚未定义持续授权协议，不提供无人确认执行。果蝇与 Jev 盯盘为独立策略，账户存在未完成计划或委托时不追加计划。

Python 神经与生活运行在独立进程。宿主仅开放账户查询、行情、模型调用和预演接口；神经进程不持有账户凭据或执行接口。模块文件随应用分发，首次使用将运行环境下载到用户目录并校验。语言模型复用已验证的 QuantStudio 配置。

关闭页面不停止后台生活；退出 Host 停止进程并保存。重启恢复记忆、家园和此前启用的交易建议，重新核对账户后等待新信号；主动停止的状态保持停止。模型与造景分别计数、限制调用，不虚构服务商费用。

验收：专用环境准备、真实模块启动、3D 资产加载、状态保存恢复；账户切换、过期行情、重复计划与成交反馈隔离；相关 Python/TypeScript 测试、宿主与客户端构建。交易连接和成交验证需区分模拟接口测试与真实柜台结果。

## 使用

1. 重启构建后的 QuantStudio，从「比赛」页果蝇卡片的「准备果蝇交易员」或侧栏进入果蝇页面，在首次设置中点击「一键准备果蝇」。控制器与神经 Python、MaleCNS 依次在后台准备，页面切换不打断准备；进度、错误和重试可见。已有 Blender 时可填写安装目录或 `blender.exe` 路径，验证后复用；也会自动识别 PATH 中的 Blender，未找到时准备专用版本。
2. 模型连接与 Jev API Key 统一在「设置 → 模型服务」管理。Jev 区域测试成功后保存到原有凭据库，比赛盯盘与果蝇共用，密钥不回显；已有密钥无需迁移。两处使用入口均提示配置路径并提供跳转。果蝇列表直接读取用户在 QuantStudio 接入并验证成功的模型，调用复用原有连接与密钥；语言 AI、Jev 和造景调用额度默认为 0，先设置额度再测试连接。只体验神经生活不需要模型 API。
3. 「设置 → 环境」默认开启「生成交易选择」。在交易配置中连接比赛账户，无需先填写合约；勾选品种后查询主力，其他月份核对后可替换。首次完成前仅检查神经环境、账户和实际合约。缺项在字段旁显示，并提供定位入口；可「保存，稍后补齐」，此时不启动交易。「比赛 → 额外风险限制」默认折叠，0 表示不额外限制，已有正数限制继续生效。原每品种目标名义金额改为每品种名义上限，不再决定固定开仓手数。
4. 连接 PandaData 后，「完成并开始交易」启用计划生成并连接行情；完整的 500 根 K 线和新鲜报价就绪后才决策。语言模型、Jev 与造景不是交易的必填项。后续保存配置不覆盖主动暂停状态；实际计划仍逐笔人工确认，停止建议不撤销已提交委托。
5. 交易首页固定为「成交与盈亏 → 决策原因 → 学习反馈」。待确认计划随成交区域展示；行情、历史下载和运行诊断在「行情与运行详情」中展开。生活场景、生活反馈和神经活动移到独立「生活」页，取消首页自由拖拽和重复统计卡片。
6. 比赛页 Jev 与果蝇卡片继续默认折叠、同步同一个后台。账户不匹配时不展示旧账户交易数据，安装与配置统一在果蝇页处理。

仓位读出（`neural-trade-3`）：方向评分与学习权重来自神经响应，目标资金比例取胜出方向对其他方向/等待阈值的相对优势，再乘以 `1 - 波动读出`。账户动态权益优先读取比赛正式字段 `totalProfit`，兼容 `equity/balance/Balance`。比赛 `contract-spec.margin` 提供多空按金额保证金率和每手保证金，按 `价格 × 乘数 × 保证金率 + 每手保证金` 估算每手占用。容量使用可用资金的 90%（留 10% 应对手续费和价格变化），并受权益按所选品种均分后的 90% 预算、每品种 500 手及用户可选名义上限约束；实际准入以比赛预演结果为准。缺失保证金或资金时等待，不猜测参数。此为工程读出规则，不代表已验证的有效策略。

目标仓位与决策时持仓一同保存，界面展示「当前 → 目标」。每次仅生成差额计划；反向证据先平旧方向，收到成交并无待处理委托后等待新决策。预演前再次核对账户、持仓、资金、可平手数、报价和可选限制；持仓发生变化则放弃旧决策。减仓不受开仓信号过滤阻挡。升级兼容 v2 读出检查点，不重放旧信号。净收益奖励按平仓周期初始账户权益归一化，不再依赖可选名义金额。

历史首次补齐 500 根已完成 K 线，之后复用持久化窗口，按原生 1 或 5 分钟周期从最早缺口或窗口尾部所属交易日期补取，休市时改为每 5 分钟检查。PandaData 接口按交易日期查询，因此最新交易日会有重叠，按时间戳合并去重；不再每轮重取整个初始日期窗口。重启后复用缓存，长期离线导致窗口过旧时重新补齐。果蝇看板与比赛卡片展开区的「获取历史数据」按钮共用同一后台任务，立即返回状态，不阻塞家园。每个合约显示已完成 K 线根数、来源最近成功时间及失败原因，切换合约或周期时不写入旧请求结果。成功获取不等于可交易，仍需通过 500 根完整性及新鲜报价检查。

历史和比赛连接各自持久化冷却状态，手动按钮、自动刷新和页面切换不能绕过。页面区分「比赛接口」和「PandaData」故障，显示重试倒计时；有服务端等待时间时遵守该时间，否则按 30、60、120、240、300 秒退避。限流或授权失效会终止本轮剩余合约请求；其他单合约失败继续处理其他合约，已有缓存与成功时间保留。行情轮询复用 15 秒账户快照，只读同步保留账户身份检查，不再反复执行完整 doctor 自检；交易预演和执行仍使用原有完整校验。

交易决策使用 PandaData 原生 1 或 5 分钟线；3 分钟只用于分析图表的显示聚合，不作为行情请求。合约不自动换月。存在待处理计划或委托时等待；Jev 盯盘运行时不生成果蝇计划。停止建议不撤销已提交委托。

历史行情通过 PandaData MCP 的 `get_future_min` 获取。返回被截断的预览时，程序自动拆分交易日期区间，完整读取各段后统一写入缓存，再校验并取最新 500 根已完成 K 线；不把预览当作完整历史。单日仍被截断或任一分段失败时保留原缓存并报告错误，修复前不绕过历史完整性检查。

手续费、出入金、柜台交易日或完整成交证据缺失时，继续保存实际账户权益和已确认成交，暂停交易奖励结算；缺失数据不按零处理。学习仅发生在读出层，神经连接组冻结。事件回放读取本地记录，不提供旧宿主的历史回测引擎。

状态位于当前 DSH 用户目录下的 `quantskills/fly`。Host 退出等待检查点保存；再次打开恢复记忆、家园及此前启用的交易建议；主动暂停保持暂停，配置不完整时停止建议，重启前的旧信号不重放。源码来源与分发说明见 `packages/quantskills-session/fly-runtime/PROVENANCE.md`。

## 验证命令

`node scripts/verify-fly-runtime.mjs` 在系统临时目录创建隔离控制器，验证资源和重启记忆。加 `--neural --scene` 会准备真实神经依赖、运行神经生活并用 Blender 构建和校验家园；可加 `--blender "安装目录"` 复用本机版本。测试不连接账户、不下单，也不调用付费模型。

2026-09-24 自主仓位与交易优先界面验证：103 项 TypeScript 测试、31 项 Python 测试通过，客户端与宿主类型检查和构建通过，源码资源检查通过。内置浏览器验证交易首页顺序、独立生活页、环境中的交易开关及缺项定位。原有授权在重启后重新校验，未登录、账户不一致或主动关闭比赛模式均不会自动恢复；不会重放旧交易信号。浏览器已核对新版比赛设置：账户连接正常、额外限制默认折叠、金额均为 0 时显示配置齐全；真实 IF/IM 行情的仓位容量计算成功。保留原交易建议暂停状态，未提交实际交易。

复跑关键测试（需项目依赖；Python 环境需要 Pydantic 和 NumPy。独立神经 Python 可单独运行 test_sizing.py，其余测试使用控制器 Python）：

```powershell
pnpm exec vitest run packages/ui-quantskills/tests/fly-trading-setup.client.spec.tsx packages/ui-quantskills/tests/fly-onboarding.client.spec.tsx packages/ui-quantskills/tests/fly-history.client.spec.tsx packages/ui-quantskills/tests/fly.client.spec.tsx packages/ui-quantskills/tests/fly-contest.client.spec.tsx packages/quantskills-session/tests/fly.spec.ts packages/quantskills-session/tests/fly-runtime-install.spec.ts packages/quantskills-session/tests/contest.spec.ts
python -m unittest discover -s packages/quantskills-session/fly-runtime -p "test_*.py"
node scripts/verify-source.mjs
```
