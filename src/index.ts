/**
 * @deepseek-ai/dsh-quantskills-plugin — an installable patch-list bundle that
 * adds the QuantSkills application to the native DSH Web shell. Runtime
 * behavior lives in `cordis.patch.yml` and the mounted domain/client plugins.
 * @module @deepseek-ai/dsh-quantskills-plugin
 */

export {}
