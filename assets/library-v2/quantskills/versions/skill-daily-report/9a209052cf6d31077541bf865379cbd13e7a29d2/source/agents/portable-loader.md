# Portable runtime adapter

Load the repository root `SKILL.md` as the skill declaration. Resolve `scripts/fetch_market_data.py` relative to the repository root, not from a user-specific home directory. Preserve the report's data-source notes and non-investment-advice disclaimer.
