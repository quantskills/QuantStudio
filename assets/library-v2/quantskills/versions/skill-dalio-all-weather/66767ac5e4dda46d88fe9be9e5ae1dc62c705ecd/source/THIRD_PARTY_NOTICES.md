# 第三方软件与数据说明

项目通过 `panda_data==0.0.12` 访问 PandaData。它是独立的第三方服务，不属于本仓库。

源码压缩包和发布包都不包含该 Python 包，安装锁定依赖时会单独下载。版本 0.0.12 的公开包信息没有写明软件许可证和源码地址，因此使用者需要自行确认账号、数据权限和具体用途是否得到服务方许可。

本仓库与 PandaData 没有隶属或背书关系；文档中提到接口，只表示技术上的兼容。
