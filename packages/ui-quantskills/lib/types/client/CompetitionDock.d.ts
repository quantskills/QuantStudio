import { type ReactNode } from 'react';
/** Keep plan confirmation mounted and accessible even when account details are folded. */
export declare function CompetitionDock({ kind, label, title, subtitle, actions, children }: {
    kind: 'contest' | 'factor-contest';
    label: string;
    title: ReactNode;
    subtitle: ReactNode;
    actions: ReactNode;
    children: ReactNode;
}): import("react").JSX.Element;
//# sourceMappingURL=CompetitionDock.d.ts.map